export enum QueueStatus {
  WAITING = "WAITING",
  IN_CONSULTATION = "IN_CONSULTATION",
  COMPLETED = "COMPLETED",
  CANCELLED = "CANCELLED",
}

export enum PrescriptionStatus {
  PENDING = "PENDING",
  PREPARED = "PREPARED",
  DISPENSED = "DISPENSED",
  CANCELLED = "CANCELLED",
}

export enum Gender {
  MALE = "MALE",
  FEMALE = "FEMALE",
}
